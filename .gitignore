# Ignore the contents of the _site directory and other cache directories
_site/
.sass-cache/

# Ignore the directory for local files during development
local/

# Ignore the Gemfile that is generated
Gemfile.lock

# Ingore files created by npm
node_modules
package-lock.json

.DS_Store
files/.DS_Store

# CV: keep only the published CV source in the repo;
# local drafts, old versions, PDFs, and LaTeX build artifacts stay local
cv/*
!cv/kirgis_peter_cv_2026.tex
texput.log